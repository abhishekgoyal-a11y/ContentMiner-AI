Please don't write code like this, forgetting to check for edge cases. Now, when we write code as a programmer, a lot of times we're optimistic. We just assume that whoever's using our code is a genius, they're going to know exactly how to use it. They're going to pass the correct input and everything's going to work in an ideal scenario. Anyone who's been writing code for a little bit of time now knows that's just not the case. And a lot of times your code can break in really weird ways that you've never predicted. You can see that we have a calculate average function. Totally simple. We grab our numbers, we loop through all the numbers, we add these together, and then we divide the total by the length of our numbers. Any normal person would read this and go, okay, yeah, that makes sense. You know, we can get a result like 20 here. We calculate the average of these numbers. But what happens if the length of my list is zero? In fact, we can run this code and notice that I actually get an error, zero division error. Now, the reason we got that error is because we divide it by the length of numbers and what the length of numbers was zero. So that's fine. But what happens if we do something like three and then maybe we pass in the string hello. And then we run this code again and notice that now we get unsupported operand type four plus equals int and string, okay, so there are errors that can occur. Even though these seem silly, when you're running code thousands, hundreds of thousands of times for so many different inputs, really anything can happen. So when you write your functions and your code, you want to be really robust and make sure you're checking for all of the possible things that could go wrong before you start executing it. So let's look at an example where we fix that. Now again, this is a little bit overkill. It's more robust. This function is going to work to calculate the average of any numbers now and it's going to handle any issues that could occur. So you can see this first line here, what we're doing is making sure that numbers is actually a list. Now in Python, we're working with a dynamically typed language. That means that even if we add a type annotation like a list here, technically people can pass anything to this function that they want. So first, I make sure numbers is actually a list. So what we're doing is removing all of the invalid instances here. So anything that's non-int or float and just keeping all of the numbers.